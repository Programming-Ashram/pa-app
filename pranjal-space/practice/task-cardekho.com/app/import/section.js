import Section from "../export/section";


let section =[ cardName, pageLink, linkName];
let sectionArea = document.getElementById('sectionArea');

section.forEach( => {
    sectionArea.innerHTML +=`
        
    `;
});